server:
  port: 7071


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YeralD21/-YeralD21-DAD-2024-G1-2.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
          #username:
          #password:
  security:
    user:
      name: root
      password: 123456